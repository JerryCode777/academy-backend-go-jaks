# Variables de entorno para desarrollo
# Renombra a .env para usar

# === OBLIGATORIAS (🚨 SIN DEFAULTS) ===
DB_PASSWORD=password123
JWT_SECRET_KEY=mi_super_secreto_jwt_key_para_desarrollo_de_32_caracteres_minimo

# === OPCIONALES (con defaults si no se especifican) ===

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_NAME=academi_dev
DB_SSLMODE=disable

# JWT Configuration  
JWT_ISSUER=academi-backend
JWT_EXPIRES_HOURS=24

# Servidor
SERVER_PORT=8080
SERVER_HOST=0.0.0.0
API_BASE_PATH=/api/v1